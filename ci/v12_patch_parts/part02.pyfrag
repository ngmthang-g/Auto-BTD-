 }\n            btd.visualLastX = s.x;\n            btd.visualLastY = s.y;\n            btd.visualMovementSeen = false;\n            btd.visualStationarySince = 0;\n            btd.visualLastConfirmProbeTick = 0;\n            BtdSetPhase(a, BtdPhase::VisualWaitTravel, now,\n                L"AUTO BTĐ • đã click tọa 2 (Đến địa điểm) • chờ di chuyển rồi đứng im >6s");\n            return;\n        }\n\n        if (btd.phase == BtdPhase::VisualWaitTravel) {\n            if (!Elapsed(now, btd.lastPollTick, 250)) return;\n            btd.lastPollTick = now;\n\n            // Popup xác nhận chuyển map được ưu tiên như core cũ. Probe chỉ gọi callback\n            // khi MessageBox thật sự có nút Confirm; không có popup thì bridge fail-closed.\n            if (BtdMutationReady(btd, now) &&\n                (btd.visualLastConfirmProbeTick == 0 || Elapsed(now, btd.visualLastConfirmProbeTick, 1000))) {\n                btd.visualLastConfirmProbeTick = now;\n                Response confirm{}; std::wstring confirmError;\n                if (a.bridge.Call(Command::ConfirmMap, 0, 0, 0, confirm, confirmError, 1800)) {\n                    BtdMarkMutation(btd, now);\n                    btd.visualStationarySince = 0;\n                    rt.status = L"AUTO BTĐ • popup chuyển map ưu tiên • đã callback Xác nhận";\n                    return;\n                }\n            }\n\n            const int dx = std::abs(s.x - btd.visualLastX);\n            const int dy = std::abs(s.y - btd.visualLastY);\n            const bool positionChanged = dx > 2 || dy > 2;\n            if (positionChanged) {\n                btd.visualMovementSeen = true;\n                btd.visualStationarySince = 0;\n                btd.visualLastX = s.x;\n                btd.visualLastY = s.y;\n                rt.status = L"AUTO BTĐ • đang di chuyển tới điểm Trừng Ác • M" +\n                            std::to_wstring(s.mapID) + L" " + std::to_wstring(s.x) + L"," + std::to_wstring(s.y);\n                return;\n            }\n            if (s.autoPathing) btd.visualMovementSeen = true;\n\n            if (!btd.visualMovementSeen) {\n                if (Elapsed(now, btd.phaseTick, 20000)) {\n                    BtdFail(a, L"TRUNG_AC_ROUTE_NO_MOVEMENT", L"click Đến địa điểm nhưng 20 giây không thấy chuyển động");\n                    return;\n                }\n                rt.status = L"AUTO BTĐ • chờ nhân vật bắt đầu di chuyển sau Đến địa điểm";\n                return;\n            }\n\n            if (btd.visualStationarySince == 0) btd.visualStationarySince = now;\n            if (Elapsed(now, btd.visualStationarySince, kBtdStationaryProofMs)) {\n                btd.visualDragCount = 0;\n                BtdSetPhase(a, BtdPhase::VisualFindOrderSecond, now,\n                    L"AUTO BTĐ • nhân vật đứng im liên tục >6s • dùng Trừng Ác Lệnh lần 2");\n                return;\n            }\n            if (Elapsed(now, btd.phaseTick, 180000)) {\n                BtdFail(a, L"TRUNG_AC_ROUTE_TIMEOUT", L"quá 180 giây chưa có proof đứng im 6 giây");\n                return;\n            }\n            rt.status = L"AUTO BTĐ • đã dừng di chuyển • đang đếm proof đứng im 6 giây";\n            return;\n        }\n\n        if (btd.phase == BtdPhase::VisualClickDestinationSecond) {\n            if (!Elapsed(now, btd.phaseTick, kBtdMinActionGapMs) || !BtdMutationReady(btd, now)) return;\n            std::wstring error;\n            if (!BtdClickFixedPoint(a, kBtdCoord2X, kBtdCoord2Y, now, error)) {\n                BtdFail(a, L"TRUNG_AC_COORD2_SECOND_FAIL", error); return;\n            }\n            BtdSetPhase(a, BtdPhase::VisualCloseBag, now,\n                L"AUTO BTĐ • đã click tọa 2 lần 2 • chờ ≥500ms rồi đóng tay nải");\n            return;\n        }\n\n        if (btd.phase == BtdPhase::VisualCloseBag) {\n            if (!Elapsed(now, btd.phaseTick, kBtdMinActionGapMs) || !BtdMutationReady(btd, now)) return;\n            std::wstring error;\n            if (!BtdClickFixedPoint(a, kBtdCoord3X, kBtdCoord3Y, now, error)) {\n                BtdFail(a, L"TRUNG_AC_CLOSE_BAG_COORD3_FAIL", error); return;\n            }\n            btd.visualFightStarted = false;\n            btd.combatBurstStartTick = 0;\n            BtdSetPhase(a, BtdPhase::VisualFight15s, now,\n                L"AUTO BTĐ • tay nải đã đóng • chờ ≥500ms rồi Auto đánh quái 15s");\n            return;\n        }\n\n        if (btd.phase == BtdPhase::VisualFight15s) {\n            if (!btd.visualFightStarted) {\n                if (!Elapsed(now, btd.phaseTick, kBtdMinActionGapMs) || !BtdMutationReady(btd, now)) return;\n                std::wstring error;\n                if ((s.validMask & ValidAutoFight) && s.autoFight) {\n                    btd.visualFightStarted = true;\n                    btd.combatBurstStartTick = now;\n                    rt.status = L"AUTO BTĐ • AutoFight đã ON • bắt đầu đếm 15s";\n                    return;\n                }\n                if (!BtdStartFight(a, error)) {\n                    rt.status = L"AUTO BTĐ • StartAutoFight thất bại • " + error; return;\n                }\n                BtdMarkMutation(btd, now);\n                btd.visualFightStarted = true;\n                btd.combatBurstStartTick = now;\n                rt.status = L"AUTO BTĐ • Auto đánh quái ON • đếm 15s";\n                return;\n            }\n\n            const DWORD fightStart = btd.combatBurstStartTick;\n            if (fightStart == 0 || !Elapsed(now, fightStart, 15000)) {\n                const DWORD elapsedFight = fightStart == 0 ? 0 : now - fightStart;\n                const DWORD remain = elapsedFight >= 15000 ? 0 : 15000 - elapsedFight;\n                rt.status = L"AUTO BTĐ • đang đánh quái • còn ~" + std::to_wstring((remain + 999) / 1000) + L"s";\n                return;\n            }\n\n            if ((s.validMask & ValidAutoFight) && s.autoFight) {\n                if (!BtdMutationReady(btd, now)) return;\n                std::wstring error;\n                if (!BtdStopFight(a, error)) {\n                    rt.status = L"AUTO BTĐ • hết 15s nhưng StopAutoFight thất bại • " + error; return;\n                }\n                BtdMarkMutation(btd, now);\n            }\n            btd.visualFightStarted = false;\n            btd.pendingTurnIn = true;\n            LogAccount(a, L"TRỪNG ÁC v1.2: AutoFight đủ 15s • về Ngô Giới trả nhiệm vụ");\n            BtdSetPhase(a, BtdPhase::GoNgoGioi, now,\n                L"AUTO BTĐ • đánh quái đủ 15s • về Ngô Giới trả nhiệm vụ");\n            return;\n        }\n\n','src/controller.cpp hunk 11')
s=repl(s,'                    rt.status = L"AUTO BTĐ • quái đã spawn nhưng SelectTarget FAIL • " + error; return;\n                }\n                if (!BtdStartFight(a, error)) { rt.status = L"AUTO BTĐ • quái đã spawn nhưng StartAutoFight FAIL • " + error; return; }\n                btd.combatBurstStartTick = static_cast<int>(now);\n                std::wstring label = candidate->name[0] ? std::wstring(candidate->name) : L"GMonster";\n                LogAccount(a, L"TRỪNG ÁC: bắt được quái mới sau Triệu hồi • " + label + L" • RoleID=" + std::to_wstring(candidate->roleID) +\n                    L" • ResID=" + std::to_wstring(candidate->resID));\n','                    rt.status = L"AUTO BTĐ • quái đã spawn nhưng SelectTarget FAIL • " + error; return;\n                }\n                if (!BtdStartFight(a, error)) { rt.status = L"AUTO BTĐ • quái đã spawn nhưng StartAutoFight FAIL • " + error; return; }\n                btd.combatBurstStartTick = now;\n                std::wstring label = candidate->name[0] ? std::wstring(candidate->name) : L"GMonster";\n                LogAccount(a, L"TRỪNG ÁC: bắt được quái mới sau Triệu hồi • " + label + L" • RoleID=" + std::to_wstring(candidate->roleID) +\n                    L" • ResID=" + std::to_wstring(candidate->resID));\n','src/controller.cpp hunk 12')
s=repl(s,'    }\n\n    void Tick() {\n        // AUTO BTĐ v1.1.1: one independent orchestrator per PID. Legacy PK/Dungeon/Trade/Sell/Train\n        // coordinators are deliberately not ticked here. Route/mount/revive primitives remain shared core.\n        std::vector<bool> snapshotReady(accounts_.size(), false);\n        for (std::size_t i = 0; i < accounts_.size(); ++i) {\n','    }\n\n    void Tick() {\n        // AUTO BTĐ v1.2: one independent orchestrator per PID. Legacy PK/Dungeon/Trade/Sell/Train\n        // coordinators are deliberately not ticked here. Route/mount/revive primitives remain shared core.\n        std::vector<bool> snapshotReady(accounts_.size(), false);\n        for (std::size_t i = 0; i < accounts_.size(); ++i) {\n','src/controller.cpp hunk 13')
write('src/controller.cpp',s)
s=read('src/bridge.cpp')
s=repl(s,'    Il2CppClass* inputSyncManager = nullptr;\n    const MethodInfo* inputSyncGetInstance = nullptr;\n    const MethodInfo* inputSyncPress = nullptr;\n    const MethodInfo* inputSyncRelease = nullptr;\n    const MethodInfo* inputSyncCancel = nullptr;\n    FieldInfo* inputSyncDragging = nullptr;\n','    Il2CppClass* inputSyncManager = nullptr;\n    const MethodInfo* inputSyncGetInstance = nullptr;\n    const MethodInfo* inputSyncPress = nullptr;\n    const MethodInfo* inputSyncDrag = nullptr;\n    const MethodInfo* inputSyncRelease = nullptr;\n    const MethodInfo* inputSyncCancel = nullptr;\n    FieldInfo* inputSyncDragging = nullptr;\n','src/bridge.cpp hunk 0')
s=repl(s,'        g_ui.inputSyncPress = ExactMethod(\n            g_ui.inputSyncManager, plan[0].methodName, plan[0].parameterCount, false,\n            "System.Int32", "UnityEngine.Vector2");\n        g_ui.inputSyncRelease = ExactMethod(\n            g_ui.inputSyncManager, plan[1].methodName, plan[1].parameterCount, false,\n            "UnityEngine.Vector2");\n','        g_ui.inputSyncPress = ExactMethod(\n            g_ui.inputSyncManager, plan[0].methodName, plan[0].parameterCount, false,\n            "System.Int32", "UnityEngine.Vector2");\n        g_ui.inputSyncDrag = ExactMethod(\n            g_ui.inputSyncManager, "UpdateUIDrag", 1, false, "UnityEngine.Vector2");\n        g_ui.inputSyncRelease = ExactMethod(\n            g_ui.inputSyncManager, plan[1].methodName, plan[1].parameterCount, false,\n            "UnityEngine.Vector2");\n','src/bridge.cpp hunk 1')
s=repl(s,'    }\n\n    const bool methodsValid = g_ui.inputSyncGetInstance && g_ui.inputSyncPress &&\n        g_ui.inputSyncRelease && g_ui.inputSyncCancel &&\n        ReturnType(g_ui.inputSyncPress, "System.Void") &&\n        ReturnType(g_ui.inputSyncRelease, "System.Void") &&\n        ReturnType(g_ui.inputSyncCancel, "System.Void");\n    if (!g_ui.inputSyncManager || !g_ui.unityScreen || !methodsValid ||\n','    }\n\n    const bool methodsValid = g_ui.inputSyncGetInstance && g_ui.inputSyncPress &&\n        g_ui.inputSyncDrag && g_ui.inputSyncRelease && g_ui.inputSyncCancel &&\n        ReturnType(g_ui.inputSyncPress, "System.Void") &&\n        ReturnType(g_ui.inputSyncDrag, "System.Void") &&\n        ReturnType(g_ui.inputSyncRelease, "System.Void") &&\n        ReturnType(g_ui.inputSyncCancel, "System.Void");\n    if (!g_ui.inputSyncManager || !g_ui.unityScreen || !methodsValid ||\n','src/bridge.cpp hunk 2')
s=repl(s,'    if (!InvokeInternalPointClick(normalizedX, normalizedY, detail, cap)) return false;\n    response.resultCode = static_cast<std::int32_t>(ActionResult::ActionInvoked);\n    SetText(detail, cap, L"InputSync click nội bộ hoàn chỉnh: TryClickUI → EndUIDrag");\n    return true;\n}\n\n','    if (!InvokeInternalPointClick(normalizedX, normalizedY, detail, cap)) return false;\n    response.resultCode = static_cast<std::int32_t>(ActionResult::ActionInvoked);\n    SetText(detail, cap, L"InputSync click nội bộ hoàn chỉnh: TryClickUI → EndUIDrag");\n    return true;\n}\n\n\nbool InvokeInternalPointDrag(int startNormalizedX, int startNormalizedY,\n                             int endNormalizedX, int endNormalizedY,\n                             wchar_t* detail, std::size_t cap) {\n    UnityVector2 start{}, end{};\n    if (!BuildInputSyncScreenPoint(startNormalizedX, startNormalizedY, start, detail, cap) ||\n        !BuildInputSyncScreenPoint(endNormalizedX, endNormalizedY, end, detail, cap)) return false;\n\n    Il2CppObject* manager = nullptr;\n    if (!InvokeObject(g_ui.inputSyncGetInstance, nullptr, manager, detail, cap) || !manager) {\n        SetText(detail, cap, L"InputSyncManager.Instance chưa sẵn sàng cho drag");\n        return false;\n    }\n    bool dragging = false;\n    if (!ReadInputSyncDragging(manager, dragging, detail, cap)) return false;\n    if (dragging) {\n        CancelOwnedInputSyncDrag(manager);\n        SetText(detail, cap, L"InputSyncManager có drag cũ; đã cancel, không chồng drag mới");\n        return false;\n    }\n\n    std::int32_t button = internal_ui_click_logic::kLeftButton;\n    void* pressArgs[] = {&button, &start};\n    if (!InvokeVoid(g_ui.inputSyncPress, manager, pressArgs, detail, cap)) {\n        CancelOwnedInputSyncDrag(manager);\n        SetText(detail, cap, L"Hidden drag: TryClickUI thất bại");\n        return false;\n    }\n    if (!ReadInputSyncDragging(manager, dragging, detail, cap) || !dragging) {\n        CancelOwnedInputSyncDrag(manager);\n        SetText(detail, cap, L"Hidden drag: điểm START không tạo được UI drag target");\n        return false;\n    }\n\n    constexpr int kSteps = 12;\n    for (int i = 1; i <= kSteps; ++i) {\n        const float t = static_cast<float>(i) / static_cast<float>(kSteps);\n        UnityVector2 point{